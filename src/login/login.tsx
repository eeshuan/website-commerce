import React from 'react';

import './login.scss';

export default class Login extends React.Component {
    constructor(public props) {
        super(props);
    }

    render () {
        return (
            <div>
                This is the login
            </div>
        );
    }
}